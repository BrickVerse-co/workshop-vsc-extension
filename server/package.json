{
  "name": "bricklua-language-server",
  "version": "0.0.1",
  "private": true,
  "main": "./out/server.js",
  "scripts": {
    "compile": "tsc -p .",
    "test": "vitest run",
    "watch": "tsc -w -p ."
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}